<%= form_for(@rute) do |f| %>
  <% if @rute.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rute.errors.count, "error") %> prohibited this rute from being saved:</h2>

      <ul>
      <% @rute.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :rute %><br />
    <%= f.text_field :rute %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
